{
  "name": "invoice-generator-frontend",
  "version": "1.0.0",
  "description": "Frontend for invoice generator",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000",
    "dev": "live-server --port=3000 --watch=.",
    "build": "echo 'Build process would be defined here'",
    "test": "jest",
    "lint": "eslint assets/js/*.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "eslint": "^8.39.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0"
  },
  "dependencies": {
    "axios": "^1.3.6"
  }
}
